spring:
  application:
    name: ordem-servico
  datasource:
    url: jdbc:mysql://localhost:3306/orderflow_os?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
    username: root
    password: "123Senai@!"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect